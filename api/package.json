{
  "name": "api",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --ci --watchAll=false",
    "swagger:json": "node scripts/export-swagger.mjs",
    "swagger:validate": "redocly lint docs/openapi.json",
    "ci:swagger": "npm run swagger:json && npm run swagger:validate"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.5",
    "jest": "^30.1.3",
    "supertest": "^7.1.4"
  }
}
